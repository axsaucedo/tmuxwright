#!/usr/bin/env node
// Future create-tmuxwright CLI entry (placeholder).
//
// The scaffolder is intentionally deferred until terminal-mode v1 is stable.

console.log('create-tmuxwright 0.0.0 (scaffolding)');
