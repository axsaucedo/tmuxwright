{
  "name": "tmuxwright-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Tmuxwright — deterministic E2E testing for terminal software.",
  "license": "MIT",
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "cargo build -p tmuxwright-engine && pnpm -r --filter tmuxwright --filter tmuxwright-test build && node --test packages/tmuxwright-test/tests/*.test.mjs",
    "typecheck": "tsc -b"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "eslint": "^9.0.0",
    "prettier": "^3.2.5",
    "typescript": "^5.4.5",
    "typescript-eslint": "^8.0.0"
  }
}
